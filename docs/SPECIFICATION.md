# FitTrack — Спецификация приложения

> Версия: 1.0  
> Последнее обновление: 2026-02-03

---

## 1. Общее описание

**FitTrack** — мобильное веб-приложение для планирования, выполнения и отслеживания интервальных тренировок. Приложение ориентировано на пользователей, практикующих HIIT, табата и круговые тренировки.

### 1.1 Целевая платформа
- Mobile-first SPA (Single Page Application)
- Оптимизировано для экранов 320–430px
- Поддержка iOS Safe Area (notch, home indicator)

### 1.2 Технологический стек
| Слой | Технология |
|------|------------|
| Frontend | React 18, TypeScript, Vite |
| Стилизация | Tailwind CSS, CSS Variables |
| Анимации | Framer Motion |
| Иконки | Lucide React |
| State | React Context API |
| Хранение | localStorage (offline-first) |
| Backend (опционально) | Supabase (PostgreSQL, Auth) |

---

## 2. Архитектура приложения

### 2.1 Структура экранов

```
┌─────────────────────────────────────┐
│           Safe Area Top             │
├─────────────────────────────────────┤
│                                     │
│         Контентная область          │
│         (скроллируемая)             │
│                                     │
├─────────────────────────────────────┤
│         Bottom Navigation           │
├─────────────────────────────────────┤
│          Safe Area Bottom           │
└─────────────────────────────────────┘
```

### 2.2 Навигация

Приложение использует **tab-based navigation** с тремя основными разделами:

| Tab | Иконка | Название | Описание |
|-----|--------|----------|----------|
| home | `Home` | Главная | Dashboard с виджетами |
| timer | `Timer` | Таймер | Простой интервальный таймер |
| profile | `User` | Профиль | Настройки пользователя |

---

## 3. Нижняя навигация (Bottom Navigation)

> ⚠️ **СТРОГАЯ СПЕЦИФИКАЦИЯ** — реализация навбара должна соответствовать описанию

### 3.1 Визуальные характеристики

```
Контейнер:
├── Позиционирование: fixed bottom, center
├── Отступ от края: 16px (mx-4)
├── Отступ снизу: 16px + safe-area-inset-bottom
├── Форма: pill (rounded-full)
├── Фон: glassmorphism (полупрозрачный с blur)
└── Padding: 8px по вертикали, 12px по горизонтали
```

### 3.2 Элементы навигации

```
Кнопка (неактивная):
├── Padding: 24px horizontal, 12px vertical
├── Форма: rounded-full
├── Цвет иконки: foreground с 50% opacity
├── Hover: foreground с 70% opacity
└── Размер иконки: 22px, strokeWidth: 1.5

Кнопка (активная):
├── Анимированный индикатор (layoutId)
├── Фон индикатора: foreground (solid)
├── Цвет иконки: primary-foreground
├── Transition: spring, bounce: 0.2, duration: 0.4
└── Z-index иконки: 10 (поверх индикатора)
```

### 3.3 Анимация переключения

При смене активного таба индикатор плавно перемещается между кнопками с использованием `layoutId` из Framer Motion. Это создаёт эффект "перетекания" активного состояния.

```typescript
// Псевдокод анимации
<motion.div
  layoutId="activeTab"
  className="absolute inset-0 bg-foreground rounded-full"
  transition={{ type: 'spring', bounce: 0.2, duration: 0.4 }}
/>
```

---

## 4. Онбординг

### 4.1 Структура

Онбординг состоит из **5 шагов** с прогресс-индикатором:

| Шаг | Тип | Данные |
|-----|-----|--------|
| 1 | Приветствие | — |
| 2 | Ввод имени | `name: string` |
| 3 | Ввод веса | `weight: number` (кг) |
| 4 | Выбор цели | `goal: 'lose' \| 'maintain' \| 'gain'` |
| 5 | Завершение | — |

### 4.2 Валидация

- **Имя**: минимум 2 символа
- **Вес**: число от 30 до 300 кг
- **Цель**: обязательный выбор одного варианта

### 4.3 Сохранение

По завершении онбординга данные сохраняются в `localStorage`:

```typescript
interface UserProfile {
  name: string;
  weight: number;
  goal: 'lose' | 'maintain' | 'gain';
  isOnboarded: boolean;
}
```

---

## 5. Главный экран (Dashboard)

### 5.1 Структура

```
┌─────────────────────────────────────┐
│ Привет, {name}!           [Share]  │
├─────────────────────────────────────┤
│                                     │
│      Виджет калорий (большой)       │
│                                     │
├─────────────────────────────────────┤
│                                     │
│      Карточка "Конструктор"         │
│                                     │
├──────────────────┬──────────────────┤
│   Активность     │   Тренировки     │
│   (кольцо)       │   (счётчик)      │
├──────────────────┴──────────────────┤
│                                     │
│      Карточка "Анализ недели"       │
│                                     │
└─────────────────────────────────────┘
```

### 5.2 Виджет калорий

**Назначение**: отображение прогресса сожжённых калорий за неделю.

```
Структура:
├── Заголовок: текущий день недели
├── Большое число: сожжённые калории за неделю
├── Прогресс-бар: процент от недельной цели
├── Метка: "{current} / {goal} ккал"
└── Подсказка: "в день нужно сжигать ~{daily} ккал"
```

**Расчёт цели**:
```typescript
// Дневная цель зависит от веса и цели пользователя
const dailyGoal = calculateDailyCalorieGoal(weight, goal);
// Диапазон: 200–600 ккал/день
// lose: +20% к базовому
// maintain: базовое значение
// gain: -20% к базовому

const weeklyGoal = dailyGoal * 7;
```

### 5.3 Карточка "Конструктор"

**Назначение**: быстрый доступ к созданию новой тренировки.

```
Содержимое:
├── Иконка: Dumbbell (glassmorphism стиль)
├── Заголовок: "Конструктор"
├── Подзаголовок: "Создай свою тренировку"
└── Действие: onClick → открытие WorkoutConstructor
```

### 5.4 Карточка "Активность"

**Назначение**: отображение активности за текущий день.

```
Содержимое:
├── Круговой прогресс (60 минут = 100%)
├── Текущие минуты активности
├── Цель: 60 мин/день
└── Визуал: заполняющееся кольцо
```

### 5.5 Карточка "Тренировки"

**Назначение**: доступ к библиотеке тренировок.

```
Содержимое:
├── Общее количество тренировок
├── Количество избранных (если > 0)
├── Badge: "{n} в избранном"
└── Действие: onClick → открытие WorkoutsList
```

### 5.6 Карточка "Анализ недели"

**Назначение**: сводка по недельной активности с возможностью детального просмотра.

```
Содержимое (превью):
├── Иконка: BarChart3
├── Заголовок: "Анализ недели"
└── Краткая статистика

Содержимое (модальное окно):
├── Сожжено калорий / Цель
├── Прогресс в процентах
├── Общее время тренировок
├── Количество сессий
└── Кнопка закрытия
```

---

## 6. Библиотека тренировок (WorkoutsList)

### 6.1 Структура

```
┌─────────────────────────────────────┐
│ ←  Тренировки                       │
├─────────────────────────────────────┤
│ [Поиск...]                          │
├─────────────────────────────────────┤
│ Избранные (если есть)               │
│ ┌─────────────────────────────────┐ │
│ │ Название тренировки      [Edit] │ │
│ │ N упражнений • MM:SS            │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ Готовые программы                   │
│ ┌─────────────────────────────────┐ │
│ │ Название тренировки      [Edit] │ │
│ │ N упражнений • MM:SS            │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 6.2 Пресеты (готовые тренировки)

| ID | Название | Описание |
|----|----------|----------|
| morning-energy | Утренняя энергия | Быстрая разминка |
| hiit-burn | HIIT Сжигание | Высокоинтенсивная |
| full-body | Всё тело | Комплексная |
| core-power | Сила кора | Пресс и спина |

### 6.3 Действия

- **Клик по карточке**: запуск тренировки (переход в таймер)
- **Кнопка Edit**: открытие в конструкторе для модификации
- **Поиск**: фильтрация по названию

---

## 7. Конструктор тренировок (WorkoutConstructor)

### 7.1 Двухэтапный процесс

```
Этап 1: Конфигурация
├── Добавление упражнений
├── Настройка параметров каждого упражнения
└── Кнопка "Далее"

Этап 2: Превью
├── Карточка предпросмотра
├── Название тренировки
├── Список упражнений (краткий)
├── Общая длительность
└── Действия: Редактировать / Избранное / Старт / Отмена
```

### 7.2 Параметры упражнения

```typescript
interface WorkoutExercise {
  id: string;           // UUID
  exerciseId: string;   // ID из справочника
  sets: number;         // 1–10, по умолчанию 3
  workTime: number;     // 10–120 сек, по умолчанию 30
  restTime: number;     // 5–60 сек, по умолчанию 15
}
```

### 7.3 Справочник упражнений

```typescript
interface Exercise {
  id: string;
  name: string;
  category: 'cardio' | 'strength' | 'flexibility' | 'core';
  met: number;  // Metabolic Equivalent of Task
}
```

**Категории**:
- `cardio`: Jumping Jacks, Burpees, High Knees, Mountain Climbers
- `strength`: Push-ups, Squats, Lunges, Plank
- `flexibility`: Stretching, Yoga poses
- `core`: Crunches, Russian Twists, Leg Raises

### 7.4 Сохранение в избранное

При нажатии "В избранное" на этапе превью:

```typescript
// Если имя дефолтное и уже есть такое — добавляем номер
if (name === 'Моя тренировка' && existsInFavorites('Моя тренировка')) {
  name = `Моя тренировка ${getNextNumber()}`;
}

addToFavorites({
  id: generateUUID(),
  name,
  exercises,
  createdAt: new Date()
});
```

### 7.5 Подтверждение отмены

При нажатии "Отмена" на этапе превью показывается диалог:
- "Уверены, что хотите отменить?"
- Кнопки: "Да, отменить" / "Назад"

---

## 8. Таймер тренировки (WorkoutTimer)

### 8.1 Состояния таймера

```typescript
type TimerPhase = 'work' | 'rest';
type TimerState = 'idle' | 'running' | 'paused' | 'complete';

interface TimerContext {
  currentExerciseIndex: number;
  currentSet: number;
  phase: TimerPhase;
  timeLeft: number;
  totalCaloriesBurned: number;
  isRunning: boolean;
  isComplete: boolean;
}
```

### 8.2 Визуальная индикация фазы

| Фаза | Цвет фона | Текст |
|------|-----------|-------|
| work | Синий градиент | "РАБОТА" |
| rest | Зелёный градиент | "ОТДЫХ" |

### 8.3 Расчёт калорий

```typescript
// Калории рассчитываются в реальном времени
const caloriesPerSecond = (MET * weight * 3.5) / 200 / 60;

// MET берётся из справочника упражнений
// Пример: Burpees = 8.0, Stretching = 2.5
```

### 8.4 Управление

| Кнопка | Действие |
|--------|----------|
| Play/Pause | Старт/Пауза таймера |
| Skip | Переход к следующему подходу/упражнению |
| Minimize | Сворачивание в виджет |
| Close | Завершение тренировки |

### 8.5 Сворачивание (Minimize)

При сворачивании:
1. Таймер продолжает работать в фоне
2. Появляется плавающий виджет на всех экранах
3. Виджет показывает: упражнение, сет, время, калории
4. Клик по виджету разворачивает таймер

### 8.6 Экран завершения

```
┌─────────────────────────────────────┐
│                                     │
│            🎉 Отлично!              │
│                                     │
│        Тренировка завершена         │
│                                     │
│         {calories} ккал             │
│         сожжено                     │
│                                     │
│         [Готово]                    │
│                                     │
└─────────────────────────────────────┘
```

### 8.7 Сброс состояния

**КРИТИЧНО**: При закрытии таймера ВСЕ состояние должно сбрасываться:
- `currentExerciseIndex = 0`
- `currentSet = 1`
- `totalCaloriesBurned = 0`
- `isComplete = false`
- `isRunning = false`

Это гарантирует, что следующая тренировка начнётся с чистого листа.

---

## 9. Простой таймер (SimpleTimer)

### 9.1 Назначение

Универсальный интервальный таймер без привязки к конкретным упражнениям.

### 9.2 Параметры

```typescript
interface SimpleTimerConfig {
  sets: number;      // 1–20
  workTime: number;  // 5–300 сек
  restTime: number;  // 5–120 сек
}
```

### 9.3 Интерфейс настройки

```
┌─────────────────────────────────────┐
│         Интервальный таймер         │
├─────────────────────────────────────┤
│                                     │
│  Подходы        [-]  5  [+]         │
│                                     │
│  Работа         [-] 30с [+]         │
│                                     │
│  Отдых          [-] 15с [+]         │
│                                     │
├─────────────────────────────────────┤
│           [Начать]                  │
└─────────────────────────────────────┘
```

### 9.4 Размер кнопок

Кнопки увеличения/уменьшения должны быть достаточно большими для удобного использования на мобильных устройствах:
- Минимальный размер: 56x56px (w-14 h-14)

---

## 10. Профиль пользователя

### 10.1 Структура

```
┌─────────────────────────────────────┐
│             Профиль                 │
├─────────────────────────────────────┤
│     [Аватар]                        │
│     {Имя пользователя}              │
├─────────────────────────────────────┤
│ Настройки                           │
│ ┌─────────────────────────────────┐ │
│ │ Вес                    {X} кг   │ │
│ │ Цель              {goal_label}  │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ [Сбросить прогресс]                 │
│ [Пройти онбординг заново]           │
└─────────────────────────────────────┘
```

### 10.2 Редактирование

- **Вес**: инлайн-редактирование с валидацией
- **Цель**: выбор из трёх вариантов (lose/maintain/gain)

### 10.3 Опасные действия

При нажатии "Сбросить прогресс" или "Пройти онбординг заново":
1. Показать диалог подтверждения
2. Объяснить последствия
3. Требовать явного подтверждения

---

## 11. Виджет активной тренировки

### 11.1 Назначение

Плавающий виджет, отображающий прогресс свёрнутой тренировки на любом экране.

### 11.2 Визуальные характеристики

```
Позиционирование:
├── Fixed bottom
├── Выше навбара (bottom: 100px + safe-area)
├── По центру экрана
└── Z-index: 40 (под навбаром)

Размер:
├── Ширина: auto (по содержимому)
├── Высота: auto
└── Padding: 16px

Фон:
├── Фаза work: синий
├── Фаза rest: зелёный
└── Transition: плавная смена цвета
```

### 11.3 Содержимое

```
┌─────────────────────────────────────┐
│ [Exercise] Set 2/3 • 00:25  [×]     │
│            {calories} ккал          │
└─────────────────────────────────────┘
```

### 11.4 Действия

- **Клик по виджету**: развернуть таймер
- **Кнопка закрытия**: подтверждение → отмена тренировки

---

## 12. Управление данными

### 12.1 Структура хранения (localStorage)

```typescript
// Ключи в localStorage
const STORAGE_KEYS = {
  USER_PROFILE: 'fittrack_user_profile',
  CUSTOM_WORKOUTS: 'fittrack_custom_workouts',
  WORKOUT_HISTORY: 'fittrack_workout_history',
};

// Структура профиля
interface UserProfile {
  name: string;
  weight: number;
  goal: 'lose' | 'maintain' | 'gain';
  isOnboarded: boolean;
}

// Структура пользовательской тренировки
interface CustomWorkout {
  id: string;
  name: string;
  exercises: WorkoutExercise[];
  createdAt: string; // ISO date
  isFavorite: boolean;
}

// История тренировок
interface WorkoutSession {
  id: string;
  workoutId: string;
  workoutName: string;
  completedAt: string;
  duration: number; // секунды
  caloriesBurned: number;
}
```

### 12.2 Синхронизация

При наличии Supabase:
- Данные дублируются в облако
- Локальное хранилище используется как кэш
- Конфликты разрешаются в пользу серверных данных

---

## 13. Дизайн-система

### 13.1 Цветовая палитра

> Дизайнер свободен в выборе конкретных оттенков, но должен придерживаться структуры токенов

```css
:root {
  /* Основные */
  --background: /* тёмный фон */;
  --foreground: /* светлый текст */;
  
  /* Акценты */
  --primary: /* основной акцент (для прогресса, CTA) */;
  --primary-foreground: /* текст на primary */;
  
  /* Вспомогательные */
  --secondary: /* вторичные элементы */;
  --muted: /* приглушённые элементы */;
  --accent: /* дополнительный акцент */;
  
  /* Градиенты (переменные для карточек) */
  --gradient-*-start/mid/end: /* цвета градиентов */;
}
```

### 13.2 Glassmorphism

Ключевой визуальный приём — glassmorphism (эффект матового стекла):

```css
.glass {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.12);
}

.glass-strong {
  background: rgba(255, 255, 255, 0.12);
  backdrop-filter: blur(30px);
  -webkit-backdrop-filter: blur(30px);
}
```

### 13.3 Типографика

```css
/* Рекомендуемый шрифт: Manrope или аналогичный геометрический sans-serif */

font-weights:
├── 200 (extralight): заголовки, крупные числа
├── 300 (light): основной текст
├── 400 (regular): акценты, метки
└── 500–600: кнопки, важные элементы

font-sizes:
├── display: 5rem / line-height: 1
├── display-sm: 3.5rem / line-height: 1
├── title: 1.5rem / line-height: 1.2
├── body: 1rem / line-height: 1.5
├── caption: 0.875rem / line-height: 1.4
└── badge: 0.75rem / line-height: 1
```

### 13.4 Скругления

```css
--radius: 1.5rem; /* базовое скругление */

/* Производные */
border-radius-lg: var(--radius);
border-radius-md: calc(var(--radius) - 4px);
border-radius-sm: calc(var(--radius) - 8px);
border-radius-xl: calc(var(--radius) + 8px);
border-radius-2xl: calc(var(--radius) + 16px);
border-radius-3xl: 2rem;
border-radius-full: 9999px; /* для pill-элементов */
```

### 13.5 Анимации

```css
/* Базовые */
transition-default: 200ms ease-out;
transition-slow: 400ms ease-out;

/* Framer Motion springs */
spring-default: { type: 'spring', bounce: 0.2, duration: 0.4 }
spring-bouncy: { type: 'spring', bounce: 0.4, duration: 0.5 }

/* Keyframes */
@keyframes fade-in: opacity 0→1
@keyframes slide-up: translateY(20px)→0 + opacity
@keyframes scale-in: scale(0.95)→1 + opacity
@keyframes glow-pulse: box-shadow пульсация
```

### 13.6 Карточки

Карточки должны иметь:
- Градиентный или стеклянный фон
- Скруглённые углы (3xl рекомендуется)
- Интерактивное состояние (hover, active)
- Плавные переходы

```css
.card-interactive {
  transition: all 200ms ease-out;
  cursor: pointer;
}

.card-interactive:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.4);
}

.card-interactive:active {
  transform: scale(0.98);
}
```

---

## 14. Адаптивность и доступность

### 14.1 Safe Areas

```css
/* Обязательная поддержка iOS notch и home indicator */
padding-top: env(safe-area-inset-top, 20px);
padding-bottom: env(safe-area-inset-bottom, 20px);
```

### 14.2 Touch Targets

- Минимальный размер интерактивных элементов: 44x44px
- Рекомендуемый размер для важных кнопок: 56x56px

### 14.3 Скрытие скроллбаров

```css
body {
  -ms-overflow-style: none;
  scrollbar-width: none;
}
body::-webkit-scrollbar {
  display: none;
}
```

### 14.4 Контраст

Все текстовые элементы должны соответствовать WCAG AA:
- Обычный текст: контраст ≥ 4.5:1
- Крупный текст: контраст ≥ 3:1

---

## 15. Состояния и обработка ошибок

### 15.1 Пустые состояния

| Контекст | Сообщение | Действие |
|----------|-----------|----------|
| Нет избранных | "Нет избранных тренировок" | "Создать тренировку" |
| Нет истории | "Пока нет завершённых тренировок" | "Начать первую" |
| Пустой поиск | "Ничего не найдено" | Очистить поиск |

### 15.2 Подтверждения

Опасные действия требуют подтверждения:
- Отмена тренировки на этапе превью
- Отмена активной тренировки
- Сброс прогресса
- Удаление из избранного

### 15.3 Валидация

- Все числовые поля имеют min/max ограничения
- Имя пользователя: 2–50 символов
- Вес: 30–300 кг
- Подходы: 1–20
- Время работы: 5–300 сек
- Время отдыха: 5–120 сек

---

## 16. Будущие возможности (Roadmap)

### 16.1 Планируемые функции

- [ ] Синхронизация с облаком (Supabase)
- [ ] История тренировок с графиками
- [ ] Достижения и бейджи
- [ ] Социальные функции (шеринг)
- [ ] Push-уведомления для напоминаний
- [ ] Аудио-сопровождение тренировок
- [ ] Интеграция с Apple Health / Google Fit
- [ ] Тёмная и светлая темы
- [ ] Кастомизация звуков таймера

### 16.2 Технический долг

- [ ] Рефакторинг Index.tsx (разбиение на хуки)
- [ ] Покрытие unit-тестами
- [ ] E2E тесты критических путей
- [ ] Оптимизация производительности анимаций

---

## Приложение A: Глоссарий

| Термин | Определение |
|--------|-------------|
| MET | Metabolic Equivalent of Task — единица измерения интенсивности упражнения |
| HIIT | High-Intensity Interval Training — высокоинтенсивные интервальные тренировки |
| Сет (подход) | Один цикл работа + отдых |
| Фаза | Текущий режим таймера (work/rest) |
| Пресет | Готовая тренировка из библиотеки |
| Glassmorphism | Визуальный стиль с эффектом матового стекла |

---

## Приложение B: API интерфейсы

### UserContext

```typescript
interface UserContextValue {
  profile: UserProfile | null;
  isOnboarded: boolean;
  customWorkouts: CustomWorkout[];
  weeklyStats: WeeklyStats;
  
  updateProfile: (profile: Partial<UserProfile>) => void;
  completeOnboarding: (profile: UserProfile) => void;
  resetOnboarding: () => void;
  
  addCustomWorkout: (workout: Omit<CustomWorkout, 'id' | 'createdAt'>) => void;
  updateCustomWorkout: (id: string, workout: Partial<CustomWorkout>) => void;
  removeCustomWorkout: (id: string) => void;
  
  addWorkoutSession: (session: WorkoutSession) => void;
}
```

---

*Документ подготовлен для команды разработки FitTrack*
