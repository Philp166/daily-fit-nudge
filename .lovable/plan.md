
# План: Унификация дизайна главной страницы

## Проблема

Главная страница стилистически выбивается из общего дизайна приложения:

| Экран | Карточки | Заголовок |
|-------|----------|-----------|
| **Profile** | `.glass rounded-3xl` | `text-title` (1.5rem) |
| **WorkoutsList** | `.glass rounded-3xl` | `text-title` (1.5rem) |
| **Dashboard** | Яркие градиенты | `text-display-sm` (3.5rem) |

Также виджет калорий не имеет контейнера — текст и прогресс-бар "висят в воздухе".

---

## Решение

Унифицировать Dashboard с остальными экранами, используя glassmorphism вместо градиентов, сохранив при этом акценты через цветные иконки и кольца прогресса.

---

## Изменения

### 1. Заголовок приветствия

**Файл:** `src/components/dashboard/DashboardView.tsx`

```
Было:    text-display-sm (3.5rem, extralight)
Станет:  text-title (1.5rem, regular)
```

### 2. Виджет калорий

**Файл:** `src/components/dashboard/CaloriesWidget.tsx`

Обернуть содержимое в glass-контейнер:

```
Было:    <motion.div className="text-center py-6">
Станет:  <motion.div className="glass rounded-3xl p-5 text-center">
```

### 3. WidgetCard — заменить градиенты на glass

**Файл:** `src/components/dashboard/WidgetCard.tsx`

Изменить компонент — убрать градиенты и использовать единый glass-стиль:

```tsx
// Было
const gradientClasses = {
  workout: 'gradient-workout',
  activity: 'gradient-activity',
  // ...
};

// Станет
// Убрать gradientClasses полностью
// Использовать единый стиль:
className="glass rounded-3xl p-5 card-interactive"
```

### 4. Карточки — добавить акцентные элементы

Чтобы карточки не стали одинаковыми, добавить цветовую дифференциацию через иконки:

| Карточка | Акцент |
|----------|--------|
| **ConstructorCard** | Иконка `Dumbbell` с градиентом primary |
| **ActivityCard** | Cyan badge или иконка |
| **WorkoutsCard** | Yellow/orange акцент |
| **AnalysisCard** | Кольцо прогресса уже есть |

### 5. Обновить все карточки

**Файлы для изменения:**

1. `src/components/dashboard/DashboardView.tsx` — уменьшить заголовок
2. `src/components/dashboard/CaloriesWidget.tsx` — добавить glass-контейнер
3. `src/components/dashboard/WidgetCard.tsx` — заменить градиенты на glass
4. `src/components/dashboard/ConstructorCard.tsx` — убрать gradient prop, добавить иконку
5. `src/components/dashboard/ActivityCard.tsx` — убрать gradient prop
6. `src/components/dashboard/WorkoutsCard.tsx` — убрать gradient prop
7. `src/components/dashboard/AnalysisCard.tsx` — убрать gradient prop

---

## Визуальный результат

```
До:
┌────────────────────────────────────┐
│ Привет, Атлет!              [Share]│  ← ОГРОМНЫЙ (56px)
│                                    │
│         1,234 ккал                 │  ← Без фона
│         ════════════               │
├────────────────────────────────────┤
│ ▓▓▓▓▓ КОНСТРУКТОР ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ │  ← Яркий фиолетовый
├──────────────────┬─────────────────┤
│ ▓▓▓ АКТИВНОСТЬ ▓ │ ▓▓▓ ТРЕНИРО ▓▓ │  ← Яркие бирюза + желтый
├──────────────────┴─────────────────┤
│ ▓▓▓▓▓▓▓ АНАЛИЗ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ │  ← Яркий синий
└────────────────────────────────────┘

После:
┌────────────────────────────────────┐
│ Привет, Атлет!              [Share]│  ← Умеренный (24px)
├────────────────────────────────────┤
│ ┌────────────────────────────────┐ │
│ │       Сожжено сегодня          │ │
│ │          1,234                 │ │  ← Glass контейнер
│ │       Цель: 500 ккал           │ │
│ │       ════════════●            │ │
│ └────────────────────────────────┘ │
├────────────────────────────────────┤
│ ┌────────────────────────────────┐ │
│ │ 💪  Конструктор            →   │ │
│ │     Создай свою тренировку     │ │  ← Glass + иконка
│ │     50+ упражнений             │ │
│ └────────────────────────────────┘ │
├──────────────────┬─────────────────┤
│ ┌──────────────┐ │ ┌─────────────┐ │
│ │ Активность   │ │ │ Тренировки  │ │
│ │              │ │ │             │ │  ← Glass карточки
│ │   45 мин     │ │ │   12        │ │
│ │   из 60 мин  │ │ │   программ  │ │
│ └──────────────┘ │ └─────────────┘ │
├──────────────────┴─────────────────┤
│ ┌────────────────────────────────┐ │
│ │ Анализ недели    [○ 75%]       │ │
│ │ 1,234 / 2,000 ккал             │ │  ← Glass + ring
│ └────────────────────────────────┘ │
└────────────────────────────────────┘
```

---

## Технические детали

### WidgetCard — упрощённый вариант

```tsx
interface WidgetCardProps {
  children: React.ReactNode;
  className?: string;
  onClick?: () => void;
  delay?: number;
}

const WidgetCard: React.FC<WidgetCardProps> = ({
  children,
  className = '',
  onClick,
  delay = 0,
}) => {
  return (
    <motion.div
      initial={{ opacity: 0, y: 30 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.5, delay, ease: 'easeOut' }}
      onClick={onClick}
      className={`glass rounded-3xl p-5 card-interactive ${className}`}
    >
      {children}
    </motion.div>
  );
};
```

### CaloriesWidget — с контейнером

```tsx
<motion.div className="glass rounded-3xl p-5 mb-4">
  {/* существующий контент */}
</motion.div>
```

### Акцентные иконки в карточках

```tsx
// ConstructorCard
<div className="w-12 h-12 rounded-2xl glass flex items-center justify-center mb-3">
  <Dumbbell size={24} className="text-primary" />
</div>
```
